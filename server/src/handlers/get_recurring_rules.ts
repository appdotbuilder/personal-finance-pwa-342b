import { type RecurringRule } from '../schema';

export async function getRecurringRules(userId: string): Promise<RecurringRule[]> {
    // This is a placeholder declaration! Real code should be implemented here.
    // The goal of this handler is fetching all active recurring rules for the user.
    // Should include rules that are due for processing.
    return Promise.resolve([]);
}